import StudentDashboardView from '../../components/dashboard/StudentDashboardView';

/**
 * Student Dashboard
 * Uses existing StudentDashboardView component
 */
const StudentDashboard = () => {
  return <StudentDashboardView />;
};

export default StudentDashboard;
