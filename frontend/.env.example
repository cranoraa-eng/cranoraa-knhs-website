# ─── Backend API ─────────────────────────────────────────────────────────────
# URL of the Django backend origin or API root (no trailing slash).
# Both http://localhost:8000 and http://localhost:8000/api are accepted.
VITE_API_URL=http://localhost:8000


